import { DiscriptionManagePipe } from './discription-manage.pipe';

describe('DiscriptionManagePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscriptionManagePipe();
    expect(pipe).toBeTruthy();
  });
});
