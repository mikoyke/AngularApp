import { ContextPipePipe } from "./context-pipe.pipe";

describe("ContextPipePipe", () => {
  it("create an instance", () => {
    const pipe = new ContextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
